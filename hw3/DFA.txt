DFA的设计相对比较简单，只需要一个循环，设置一个当前状态，然后每次顺序读取字符串的当前字符，
从move表中获取当前状态在输入该字符后应当转移到哪个状态，然后对应的将当前状态更新（在这里为
了程序的健壮性，我们先判断当前状态在接受输入字符后能否转移），一直循环，直到读到字符串末尾，
根据DFA的性质，当前状态应该只有一个，然后我们拿这个状态去和所有终止状态进行比较，一旦匹配成
功，输出YES，否则输出NO。